package Experiment;

import javafx.scene.paint.Color;

public interface ThePaletteDelegate {
    void changeBackgroundColor(Color color);
    void changePenColor(Color color);
    void changeTurtleImage(String image);
}
